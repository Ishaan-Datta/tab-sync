{
  "name": "onetab-reimplemented",
  "version": "2.14.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:chrome": "wxt -b chrome",
    "build:chrome": "wxt build -b chrome",
    "postinstall": "wxt prepare",
    "format:concatenated": "prettier --write \"ext-onetab-concatenated-sources-*.js\"",
    "check:concatenated": "prettier --check \"ext-onetab-concatenated-sources-*.js\"",
    "typecheck": "tsc --noEmit",
    "test:e2e": "npm run build:chrome && ONETAB_CANDIDATE_EXTENSION_PATH=.output/chrome-mv3 playwright test",
    "test:e2e:headed": "npm run build:chrome && ONETAB_CANDIDATE_EXTENSION_PATH=.output/chrome-mv3 playwright test --headed",
    "test:e2e:source": "ONETAB_CANDIDATE_EXTENSION_PATH=. playwright test tests/equivalence.spec.ts"
  },
  "devDependencies": {
    "tinyglobby": "^0.2.15",
    "typescript": "^5.9.3",
    "wxt": "^0.20.20",
    "@babel/parser": "^7.29.7",
    "@playwright/test": "^1.59.1",
    "playwright-core": "^1.59.1",
    "prettier": "^3.8.1"
  },
  "trustedDependencies": [
    "esbuild"
  ]
}
